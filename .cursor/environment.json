{
  "name": "DeepGuard AI",
  "install": "./scripts/cloud-agent-install.sh",
  "terminals": [
    {
      "name": "app",
      "command": "cd frontend && npm run build && cd ../backend && ../.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000",
      "description": "DeepGuard UI + API on port 8000"
    }
  ],
  "ports": [
    {
      "name": "DeepGuard",
      "port": 8000
    }
  ]
}
